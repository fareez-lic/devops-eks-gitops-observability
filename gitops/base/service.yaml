apiVersion: v1
kind: Service
metadata:
  name: gitops-observability-demo
  namespace: gitops-demo
  labels:
    app.kubernetes.io/name: gitops-observability-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gitops-observability-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
